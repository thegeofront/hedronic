

export interface U16Matrix {
    width: number,
    data: Uint16Array,
}

export interface FloatMatrix {
    width: number,
    data: Float32Array,
}

export interface DoubleMatrix {
    width: number,
    data: Float64Array,
}